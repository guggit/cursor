---
description: Reco NG 微应用前端开发提示词（基于 src/micro-app/apps/account 示例）
alwaysApply: true
---

> 本规则文档用于指导基于 Reco NG 微应用体系的新功能模块代码生成与重构，  
> 参考示例模块 `src/micro-app/apps/account`，产出 **可直接运行的前端微应用模块代码**。

---

## 一、总体约定

- **技术栈与定位**
  - 前端框架：**Angular**（与现有工程保持版本一致）
  - UI 组件库：**ng-zorro-antd** + Reco 自研组件（如 `@reco/core`、`@reco/ui-dynamic-page` 等）
  - 本规则仅覆盖：`src/micro-app/apps/<feature>` 目录下的**前端微应用模块**结构与代码模板。

- **模块命名约定**
  - `<feature>`：业务功能标识，用**小写单词 + 中划线**组合，如 `account`、`role-manage`，需要去除前缀，如 sys_user → user。
  - `Feature`：`<feature>` 转为 **PascalCase** 后的类名前缀，如 `account` → `Account`，`role-manage` → `RoleManage`。
  - `FEATURE_CODE`：用于权限/页面编码的**全大写下划线**形式，如 `ACCOUNT_LIST`。

- **生成目标**
  - 为每个 `<feature>` 自动生成一套前端微应用代码：
    - 路由模块：`<feature>.module.ts`
    - 业务服务：`service/<feature>.service.ts`
    - 服务模块：`service/index.ts`（`Bi<Feature>ServiceModule`）
    - 页面模块：`<feature>/index.ts`（`Bi<Feature>Module`）
    - 业务组件：
      - 列表容器：`<feature>/<feature>.container.{ts,html}`
      - 详情抽屉：`<feature>/<feature>.slider.{ts,html}`
      - 编辑弹窗：`<feature>/<feature>.edit.{ts,html}`

---

## 二、目录结构与文件用途（参考 account 模块）

以示例模块 `account` 为基准，一个完整前端微应用模块的目录结构如下：

```text
src/micro-app/apps/<feature>/
├─ service/
│  ├─ <feature>.service.ts            # 业务服务，封装当前资源 HTTP 调用
│  └─ index.ts                        # 服务 NgModule（Bi<Feature>ServiceModule）
└─ <feature>/
   ├─ <feature>.module.ts             # 路由模块，负责挂载当前微应用
   ├─ <feature>.container.ts          # 列表容器组件（CoreContainer 派生）
   ├─ <feature>.container.html        # 列表页模板（搜索 + 表格 + 抽屉触发入口）
   ├─ <feature>.slider.ts             # 详情抽屉组件（CoreView 派生）
   ├─ <feature>.slider.html           # 详情页模板（视图展示为主）
   ├─ <feature>.edit.ts               # 编辑弹窗组件（CoreEdit 派生）
   ├─ <feature>.edit.html             # 编辑表单模板
   └─ index.ts                        # NgModule（Bi<Feature>Module）
```

### 2.1 `<feature>.module.ts`

- **职责**：定义 Angular 路由，将当前功能模块以懒加载方式接入主应用。
- **示例（参考 account.module.ts）**：

```typescript
import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

import { BiAccountModule, AccountContainerComponent } from '@reco/account';

const routes: Routes = [{ path: '', component: AccountContainerComponent }];

@NgModule({
  imports: [BiAccountModule, RouterModule.forChild(routes)],
})
export class AppRouteModule {}
```

> 生成新模块时，将上述代码中的 `Account` / `@reco/account` 等替换为对应的 `<Feature>` / `@reco/<feature>`。

### 2.2 `service/<feature>.service.ts` 与 `service/index.ts`

- **职责**：
  - `<feature>.service.ts`：封装当前业务的 HTTP 请求，继承 `CoreService`。
  - `index.ts`：定义 `Bi<Feature>ServiceModule`，统一提供服务。

- **示例 service/account.service.ts**：

```typescript
import { Injectable } from '@angular/core';
import { CoreService } from '@reco/core';

@Injectable()
export class AccountService extends CoreService {
  constructor() {
    // 这里的路径是后端 WebApi 的基础路由前缀
    super('authorization/account');
  }
}
```

- **示例 service/index.ts**：

```typescript
import { NgModule } from '@angular/core';
import { BiCoreModule } from '@reco/core';

import { AccountService } from './account.service';

export { AccountService };

@NgModule({
  imports: [BiCoreModule],
  providers: [AccountService],
})
export class BiAccountServiceModule {}
```

> 生成新模块时，将 `Account` / `'authorization/account'` 替换为对应的业务名称与后端路径。

### 2.3 `<feature>/index.ts`（Bi<Feature>Module）

- **职责**：声明前端页面所需组件，导入核心模块和 UI 组件模块，导出主组件集合。
- **示例（参考 <feature>/index.ts）**：

```typescript
import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule } from '@angular/forms';

import { NzCardModule } from 'ng-zorro-antd/card';
// ... 省略若干 ng-zorro-antd 模块 ...

import { BiCoreModule } from '@reco/core';
import { BiUIDynamicPageModule } from '@reco/ui-dynamic-page';

import { BiAccountServiceModule } from '@reco/account-service';

import { AccountContainerComponent } from './account.container';
import { AccountSliderComponent } from './account.slider';
import { AccountEditComponent } from './account.edit';

export { AccountContainerComponent, AccountSliderComponent, AccountEditComponent };

@NgModule({
  imports: [
    BiCoreModule,
    BiUIDynamicPageModule,
    CommonModule,
    BiAccountServiceModule,
    FormsModule,
    NzCardModule,
    // ... 其余 ng-zorro 模块 ...
  ],
  declarations: [AccountContainerComponent, AccountSliderComponent, AccountEditComponent],
})
export class BiAccountModule {}
```

> 新模块时，需要根据业务页面实际使用的 UI 控件增减 `ng-zorro-antd` 模块，但保持 **风格与示例一致**。

### 2.4 列表容器组件：`<feature>/<feature>.container.ts` / `.html`

- **TypeScript 职责**：
  - 继承 `CoreContainer<<Feature>Service>`。
  - 负责搜索条件、分页、子视图触发（如详情抽屉）。

- **示例 TS（AccountContainerComponent）**：

```typescript
import { Component, OnInit } from '@angular/core';
import { CoreContainer, BiLayoutState } from '@reco/core';

import { AccountService } from '@reco/account-service';
import { AccountSliderComponent } from './account.slider';

@Component({
  selector: 'bi-account-container',
  templateUrl: './account.container.html',
})
export class AccountContainerComponent extends CoreContainer<AccountService> implements OnInit {
  sliderComponent = AccountSliderComponent;

  constructor(service: AccountService, layoutState: BiLayoutState) {
    super(service, 'account-container', layoutState);
  }

  ngOnInit() {
    super.ngOnInit();
    this.activeSearch();
  }
}
```

- **示例 HTML（片段，列表 + 抽屉触发）**：

```html
<bi-views range="container" bi-dynamic-page="ACCOUNT_LIST">
  <bi-search #searcher>
    <bi-dynamic-search>
      <bi-search-item *bi-dynamic-search-item="'key'" label="关键字">
        <input
          nz-input
          [(ngModel)]="searchParams.key"
          (ngModelChange)="delaySearch()"
          search-field
          placeholder="姓名 | 用户名 | 手机 | 邮箱"
        />
      </bi-search-item>
    </bi-dynamic-search>
  </bi-search>

  <bi-views range="body" bi-dynamic-table #table>
    <nz-table bi-table>
      <thead>
        <tr bi-dynamic-tr>
          <th nzShowCheckbox nzWidth="65px"></th>
          <!-- 动态列示例 -->
          <th *bi-dynamic-th="'realName'" nzWidth="18%" bi-orderby="realName">姓名</th>
          <!-- ... 其他列 ... -->
        </tr>
      </thead>
      <tbody>
        <tr
          (click)="slider.showSlider($event, account)"
          bi-dynamic-tr
          *ngFor="let account of pageInfo?.items"
        >
          <!-- 单元格示例 -->
          <td *bi-dynamic-td="'realName'">{{ account.realName || account.nickName }}</td>
        </tr>
      </tbody>
    </nz-table>
  </bi-views>
</bi-views>

<ng-container #slider="biDrawer" [biDrawer]="sliderComponent" [autoTrigger]="false"></ng-container>
```

### 2.5 详情抽屉组件：`<feature>/<feature>.slider.ts` / `.html`

- **TS 职责**：继承 `CoreView<<Feature>Service>`，用于展示单条记录详情，并关联编辑组件。
- **示例 TS**：

```typescript
import { Component } from '@angular/core';
import { CoreView, BiLayoutState } from '@reco/core';

import { AccountService } from '@reco/account-service';
import { AccountEditComponent } from './account.edit';

@Component({
  selector: 'bi-account-slider',
  templateUrl: './account.slider.html',
})
export class AccountSliderComponent extends CoreView<AccountService> {
  editComponent = AccountEditComponent;

  constructor(protected accountService: AccountService, layoutState: BiLayoutState) {
    super(accountService, 'account-slider', layoutState);
  }
}
```

- **示例 HTML（片段）**：

```html
<ng-template #header let-context>
  <bi-header>
    <ng-container head-title>
      {{ info.account?.realName || info.account?.nickName }}
    </ng-container>
    <a nz-button nzType="primary" (click)="edit.showDialog(info.account)">修改</a>
  </bi-header>
</ng-template>

<div class="card-list" bi-dynamic-page="ACCOUNT_VIEW">
  <nz-card nzTitle="基本信息">
    <bi-dynamic-view-area scope="account">
      <nz-descriptions [biColumn]="3">
        <nz-descriptions-item bi-dynamic-view-item="loginName" nzTitle="用户名" *ngIf="info.account?.loginName">
          {{ info.account?.loginName }}
        </nz-descriptions-item>
        <!-- ... 其他字段展示 ... -->
      </nz-descriptions>
    </bi-dynamic-view-area>
  </nz-card>
</div>

<ng-container #edit="biModal" [biModal]="editComponent" bi-title="编辑用户信息" [autoTrigger]="false"></ng-container>
```

### 2.6 编辑组件：`<feature>/<feature>.edit.ts` / `.html`

- **TS 职责**：继承 `CoreEdit<<Feature>Service>`，包含前端校验规则 `vms`。
- **示例 TS**：

```typescript
import { Component, OnInit } from '@angular/core';
import { CoreEdit, BiLayoutState } from '@reco/core';

import { AccountService } from '@reco/account-service';

@Component({
  selector: 'bi-account-edit',
  templateUrl: './account.edit.html',
})
export class AccountEditComponent extends CoreEdit<AccountService> implements OnInit {
  // vms 对应后端 VM 字段的前端校验配置
  vms = {
    realName: {
      NotBlank: {
        message: '姓名不能为空',
      },
    },
    loginName: {
      NotBlank: {
        message: '用户名不能为空',
      },
    },
  };

  constructor(protected accountService: AccountService, layoutState: BiLayoutState) {
    super(accountService, 'account-edit', layoutState);
  }
}
```

> `account.edit.html` 一般为常规 `nz-form` 表单，字段与后端 VM 对齐，此处不赘述。

---

## 三、基于 reco-ng.mdc 生成新模块的提示词规范

在使用本规则自动生成新模块代码时，请遵循以下交互与提示词约定。

### 3.1 必要输入参数

生成前，必须向用户确认以下信息（可通过对话或表单收集）：

1. **模块标识 `<feature>`**
   - 例如：`account`、`role-manage`、`park-config`。
2. **业务显示名称**
   - 用于页面标题、按钮文案等，例如：`账号管理`、`角色管理`。
3. **后端 WebApi 基础路由前缀**
   - 与后端 Spring Boot 控制器的 `@RequestMapping` 尾部保持一致，例如：`authorization/account`。
4. **是否需要详情抽屉 / 编辑弹窗**
   - 若仅为简单列表，可选择不生成 `slider` / `edit`，但默认 **全部生成**。

### 3.2 生成步骤（逻辑流程）

生成新模块代码的逻辑应为：

1. **解析标识与类名前缀**
   - 根据 `<feature>` 计算：
     - `Feature`（PascalCase）→ 作为类名前缀，如 `Account`。
     - `featureRouteToken`（kebab-case，通常与 `<feature>` 一致）。
2. **创建目录结构**
   - 在 `src/micro-app/apps/` 下创建 `src/micro-app/apps/<feature>/`。
   - 同时创建其下的 `service/` 与 `<feature>/` 目录。
3. **生成服务层代码**
   - 在 `service/` 下生成：
     - `<feature>.service.ts`（继承 `CoreService`，构造函数中调用 `super('<backend-path>')`）。
     - `index.ts`（`Bi<Feature>ServiceModule`，`providers` 中注册 `<Feature>Service`）。
4. **生成页面模块与组件**
   - 在 `<feature>/` 下生成：
     - `<feature>.container.ts` / `.html`
     - `<feature>.slider.ts` / `.html`
     - `<feature>.edit.ts` / `.html`
     - `index.ts`（`Bi<Feature>Module`）
   - 模板内容严格参考 `account` 示例，仅替换：
     - 选择器前缀：`bi-<feature>-container` 等；
     - 组件类名：`<Feature>ContainerComponent` 等；
     - `bi-dynamic-page` / 权限编码：如 `ACCOUNT_LIST` → `FEATURE_CODE_LIST`；
     - 列表字段、详情字段、表单字段根据实际业务字段做适配。
5. **生成路由模块**
   - 在 `src/micro-app/apps/<feature>/` 根目录生成 `<feature>.module.ts`：
     - 引入 `Bi<Feature>Module` 与 `<Feature>ContainerComponent`；
     - `Routes` 中配置 `{ path: '', component: <Feature>ContainerComponent }`；
     - 导出 `AppRouteModule` 类。

### 3.3 提示词模板示例（给代码生成智能体使用）

当你需要基于本 `reco-ng.mdc` 生成新模块时，可以使用类似如下的中文提示词（可由工具自动拼装）：

> 根据 reco-ng.mdc 中的前端微应用规范，  
> 以 `src/micro-app/apps/account` 模块为模板，  
> 为功能模块「{{业务显示名称}}」生成一个新的前端微应用模块，标识为 `<feature> = "{{feature}}"`，  
> 后端 WebApi 基础路由前缀为 `"{{backendPath}}"`。  
> 请在 `src/micro-app/apps/{{feature}}` 下创建完整目录结构及以下文件：
> - `{{feature}}.module.ts`
> - `service/{{feature}}.service.ts`
> - `service/index.ts`
> - `<feature>/{{feature}}.container.{ts,html}`
> - `<feature>/{{feature}}.slider.{ts,html}`
> - `<feature>/{{feature}}.edit.{ts,html}`
> - `<feature>/index.ts`
>
> 要求：
> 1. 所有 TypeScript 代码参考 `account` 模块的写法，使用 `CoreService/CoreContainer/CoreView/CoreEdit` 等基类。
> 2. 所有组件与服务类名统一以 `{{Feature}}` 为前缀，例如：`{{Feature}}Service`、`{{Feature}}ContainerComponent`。
> 3. 模板中的选择器、`bi-dynamic-page` 编码、按钮文字等根据「{{业务显示名称}}」进行合理命名。
> 4. 列表字段、详情字段和编辑表单字段的命名与类型，应与你后端提供的 VM 字段保持一致。

> 通过以上提示词和本文件规范，智能体应能**自动生成结构完整、风格统一**的新前端模块代码。

